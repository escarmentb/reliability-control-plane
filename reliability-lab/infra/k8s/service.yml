apiVersion: v1
kind: Service
metadata:
  name: reliability-lab
  labels:
    app: reliability-lab
spec:
  type: ClusterIP
  selector:
    app: reliability-lab
  ports:
    - name: http
      port: 80
      targetPort: 8000

